(function testDomain(domain) {


    if ( domain == "target_au") {

        // waits for the rest of the scripts to run
        window.addListener('load', Run, false)
    } else {
        Run()
    }
})()


const Run = () => {

    // run the function from the preview script to find the element

    // inject the button

}

